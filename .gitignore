# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Local temp / build artifacts
_build_*.py
*.log

# Generated by scripts/build_pharma_report.py from pharma-prices.html plus the
# committed JSON, so it is a build output here rather than a source. The private
# tracker repo DOES commit its copy - there is no Pages host there, and the
# baked file is the only way the report can be read.
pharma-report.html

__pycache__/

# LinkedIn drafts and posting notes are kept locally, not published
notes/pharma-linkedin-post.md
notes/linkedin-post-ready.txt
notes/linkedin-POST-THIS.txt
